server {
    listen 80;
    server_name localhost;

    # 重定向到 HTTPS
    # return 301 https://$server_name$request_uri;

    # 日志
    access_log /var/log/nginx/journaling-materials-hub-access.log;
    error_log /var/log/nginx/journaling-materials-hub-error.log;

    # 代理配置
    location / {
        proxy_pass http://server:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 90;
        proxy_connect_timeout 90;
    }

    # 静态文件缓存
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 健康检查
    location /health {
        proxy_pass http://server:3000/health;
        access_log off;
    }
}

# HTTPS 配置（取消注释并配置证书后使用）
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
#
#     # SSL 证书
#     ssl_certificate /etc/nginx/ssl/cert.pem;
#     ssl_certificate_key /etc/nginx/ssl/key.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # 日志
#     access_log /var/log/nginx/journaling-materials-hub-ssl-access.log;
#     error_log /var/log/nginx/journaling-materials-hub-ssl-error.log;
#
#     # 代理配置
#     location / {
#         proxy_pass http://server:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#         proxy_read_timeout 90;
#         proxy_connect_timeout 90;
#     }
#
#     # 静态文件缓存
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#
#     # 健康检查
#     location /health {
#         proxy_pass http://server:3000/health;
#         access_log off;
#     }
# }
